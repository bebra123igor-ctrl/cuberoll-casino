[phases.setup]
nixPkgs = ["nodejs_20", "python311", "gcc"]

[phases.install]
cmds = [
    "npm install",
    "python3 -m pip install -U telethon aiohttp python-dotenv"
]

[start]
cmd = "node server.js & python3 gift_manager.py"
